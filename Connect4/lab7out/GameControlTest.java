package lab7out;

import static org.junit.Assert.*;

import java.util.Random;

import javax.swing.JButton;

import org.junit.Before;
import org.junit.Test;

public class GameControlTest {
	
	/* private GamePanel gp1;
	 private GamePanel gp2;
	 private GameControl cc1;
	 private GameControl cc2;
	 
	 @Before
	  public void setUp() throws Exception
	  {
	    ChatClient user1 = new ChatClient();
	    ChatClient user2 = new ChatClient();
	    gp1 = new GamePanel(cc1);
	    gp2 = new GamePanel(cc2);
	    cc1 = new GameControl(gp1,user1);
	    cc2 = new GameControl(gp2,user2);
	       
	  }



	  @Test
	  public void testActionPerformed()
	  {
	    //Generate two 4 digit numbers - one for left/one for right
	    int i = 0;
	    String left = new String();
	    String right = new String();
	    String[] operators = {"0","1","2","3","4","5","6"};
	    String operator;
	    JButton button;
	    double expected = 0;
	    
	    Random rand = new Random();

	    int rNum = rand.nextInt(7);
	 
	//    button = gp1.getButtonAt(operatorRow, operatorCol);
	    button.doClick(1000);
	   
	   
	   

	    
	    //Compare with expected
	    if (operator.equals("+"))
	    {
	      expected = Double.parseDouble(left) + Double.parseDouble(right);
	    }
	    else if (operator.equals("-"))
	    {
	      expected = Double.parseDouble(left) - Double.parseDouble(right);
	    }
	    else if (operator.equals("*"))
	    {
	      expected = Double.parseDouble(left) * Double.parseDouble(right);
	    }
	    else if (operator.equals("/"))
	    {
	      expected = Double.parseDouble(left) / Double.parseDouble(right);
	    }
	    
	    assertEquals("check CalculateController " + left + operator + right, expected, 0,0.1);

	  }*/
	}
	  

	

